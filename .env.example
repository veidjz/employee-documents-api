PORT=3000

# From the host, the replica set advertises mongo:27017, which does not resolve outside compose
MONGO_URL=mongodb://localhost:27017/inmeta?directConnection=true

# Inside compose, docker-compose.yml sets this value instead
# MONGO_URL=mongodb://mongo:27017/inmeta?replicaSet=rs0
