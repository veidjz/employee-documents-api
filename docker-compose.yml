services:
  mongo:
    image: mongo:8
    command: ['mongod', '--replSet', 'rs0', '--bind_ip_all']
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test:
        [
          'CMD',
          'mongosh',
          '--quiet',
          '--eval',
          "try { rs.status() } catch (error) { rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'mongo:27017' }] }) }; if (!db.hello().isWritablePrimary) { quit(1) }",
        ]
      interval: 5s
      retries: 20
      start_period: 10s

  api:
    build: .
    environment:
      MONGO_URL: mongodb://mongo:27017/inmeta?replicaSet=rs0
    ports:
      - '3000:3000'
    depends_on:
      mongo:
        condition: service_healthy

volumes:
  mongo-data:
